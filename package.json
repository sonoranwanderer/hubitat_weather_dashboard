{
  "name": "ecowitt-weather-hubitat-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "./tests/run-all.sh",
    "build": "node ./build/build-dashboard.js",
    "build:watch": "node ./build/build-dashboard.js --watch",
    "build:release": "node ./build/build-dashboard.js --minify"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
